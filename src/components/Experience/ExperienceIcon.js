import React from 'react';
import SectionIcon from '../SectionIcon/SectionIcon';

const ExperienceIcon = ({ src, alt }) => {
  return (
    <SectionIcon src={src} alt={alt} className="experience-logo" />
  );
};

export default ExperienceIcon;
